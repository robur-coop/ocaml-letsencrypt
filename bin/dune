(executable
  (name oacmel)
  (public_name oacmel)
  (package letsencrypt)
  (modules oacmel)
  (libraries letsencrypt ptime.clock.os cohttp-lwt-unix fpath bos randomconv cmdliner nocrypto.unix fmt.cli fmt.tty logs.fmt logs.cli))
